import { TitleHighlightDirective } from './title-highlight.directive';

describe('TitleHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
